{
    "name": "DevDay",
    "version": "0.0.1",
    "description": "DevDay Git Talk CI",
    "private": true,
    "author": "Gerardo Di Iorio",
    "license": "ISC",
    "scripts": {
        "dev": "harp server & browser-sync start --proxy 'localhost:9000' --files 'public/**/*.jade,public/**/*.ejs,public/**/*.css,public/**/*.md, public/**/*.less, public/**/_data.json'"
    },
    "devDependencies": {
    "harp": "^0.25.x",
    "browser-sync": "^2.18.x"
    }
}
